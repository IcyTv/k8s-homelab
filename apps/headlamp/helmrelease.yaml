apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: headlamp
  namespace: headlamp
spec:
  interval: 5m
  chart:
    spec:
      chart: headlamp
      sourceRef:
        kind: HelmRepository
        name: headlamp
        namespace: flux-system
      version: "0.36.0"
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  releaseName: headlamp
  targetNamespace: headlamp
  values:
    ingress:
      enabled: true
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt
        traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
      hosts:
        - host: headlamp.icytv.de
          paths:
            - path: /
              pathType: InplementationSpecific
      tls:
        - secretName: headlamp-tls
          hosts:
            - headlamp.icytv.de
    config:
      watchPlugins: true
    pluginsManager:
      enabled: true
      configContext: |
        plugins:
          - name: headlamp-plugin-flux
        installOptions:
          parallel: true
          maxConcurrent: 2
      baseImage: node:lts-alpine
      version: latest
    persistentVolumeClaim:
      enabled: true
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 1Gi